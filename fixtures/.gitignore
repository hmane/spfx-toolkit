# Fixture build artifacts — regenerated by `npm run fixtures:verify`, not committed.
*/node_modules/
*/dist/
*/stats.json
last-verify-report.json
# Lockfiles pin a per-build toolkit tarball integrity (causes staleness/churn) — not committed.
*/package-lock.json
