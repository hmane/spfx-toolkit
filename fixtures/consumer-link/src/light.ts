// Lightweight-only entry: the toolkit's Card pulls Fluent UI + its own CSS, no PnP.
// This bundle size is the baseline Phase 1's augmentation refactor must NOT regress.
import { Card } from 'spfx-toolkit/components/Card';
export const used = { Card };
